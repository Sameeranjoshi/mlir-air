*(Original URL: https://sdk.cerebras.net/csl/tutorials/gemv-01-complete-program/running.html)*

---

We can run the program using `cs_python`, which wraps the
Cerebras-provided Python instance for executing host code.

``` bash
$ cs_python run.py --name out
```

You should see a `SUCCESS!` message at the end of execution. You have
successfully run your first program!
