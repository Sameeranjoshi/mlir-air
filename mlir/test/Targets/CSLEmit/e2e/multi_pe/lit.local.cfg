# Assign Cerebras SDK simulator tests to a named parallelism group so that
# lit caps concurrency (see parallelism_groups in lit.cfg.py).  Running many
# cslc/cs_python invocations simultaneously overwhelms the Singularity-hosted
# simulator and causes intermittent failures.
config.parallelism_group = "cerebras-sim"
