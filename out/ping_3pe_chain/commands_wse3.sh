#!/usr/bin/env bash
# Generated by air-translate --emit-csl
# One-command runner: cslc compile + cs_python host driver.
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$DIR"
cslc layout.csl --arch=wse3 \
     --fabric-dims=10,3 \
     --fabric-offsets=4,1 \
     -o compiled --memcpy --channels 1
cs_python run.py --name=compiled
